<Window x:Class="JKWatcher.ConfigStringsViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JKWatcher"
        mc:Ignorable="d"
        Title="ConfigStrings" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <!--<RowDefinition Height="25"></RowDefinition>-->
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <!--<WrapPanel Grid.Row="0" Grid.ColumnSpan="2">
           <Button>View as infostring</Button>
        </WrapPanel>-->
        <DockPanel Grid.Row="0">
            <DataGrid FontSize="10" IsReadOnly="True"  CanUserAddRows="False" DockPanel.Dock="Bottom" VerticalScrollBarVisibility="Auto" x:Name="configStringListGrid" AutoGenerateColumns="True">

            </DataGrid>
        </DockPanel>
        <DockPanel Grid.Row="0" Grid.Column="1">
            <DataGrid FontSize="10" IsReadOnly="True"  CanUserAddRows="False"  VerticalScrollBarVisibility="Auto" AutoGenerateColumns="False" ItemsSource="{Binding ElementName=configStringListGrid,Path=SelectedItem.infoStringValues}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Key" Binding="{Binding Key}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Window>
